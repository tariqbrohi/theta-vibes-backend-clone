const express = require("express");
const router = express.Router();

const mediaController = require("../controllers/media.controller");

// router.get("/media/:channel", mediaController.createComment);
// router.get("/media/:category", mediaController.channels);
// router.get("/media", mediaController.channels);

module.exports = router;
