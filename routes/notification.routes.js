const express = require("express");
const router = express.Router();

const notificationController = require("../controllers/notification.controller");

// router.post("/create-notification", notificationController.channels);
// router.put("/update-notification", notificationController.channels);
// router.get("/user-notifications", notificationController.channels);

module.exports = router;
