const express = require("express");
const router = express.Router();

const authController = require("../controllers/auth.controller");

// router.post("/sigin", authController.signin);
// router.post("/signup", authController.signin);
// router.get("/verify-email", authController.signin);
// router.post("/forgot-password", authController.signin);
// router.post("/reset-password", authController.signin);

module.exports = router;
